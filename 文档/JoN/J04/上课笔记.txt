java中，字符串比较是否相等，不能使用==做比较。必须使用.equals()
语法：

字符串.equals(字符串);==返回布尔类型
多分支结构。
switch-case结构 

switch(变量){
	case 常量1:
		 代码块;
	case 常量2:
		 代码块2;
	case ……
	default:
	其他情况

}



switch适合做等值判断。ifelseif适合做区间判断。case后面只能跟常量。且值不能重复。
case后面一般都要加break进行隔断

JDK 1.8
扫描仪+随机器

Random r=new Random();
r.nextInt();//整数范围内的随机数
r.nextInt(N);//产生0~N-1的随机数 
//产生N~M的随机数










垂直领域





---------------王先杰相亲记-------
1-你是男是女。
2-你们家几口人。人数>=3
3-年龄。age<19 &&age>14
4-三围和。sum>=240
5-体重。tz<=120
6-身高 。1.60~1.70
在一起
